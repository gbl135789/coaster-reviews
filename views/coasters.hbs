
<h2>Coasters</h2>

<ul>
    {{#each coasters}}
        <li>
            <a href="/{{park.slug}}/{{slug}}">{{name}}</a> Rating: {{rating}}
        </li>
    {{else}}
        <li>No coasters to display.</li>
    {{/each}}
</ul>